// Make a program that prints all prime numbers between 0 and 100.

#include <stdio.h>

int main(void)
{
  int counter;
  
  for(counter = 0; counter <= 100; counter++)
  {
    printf("%d ", counter);
  }
}